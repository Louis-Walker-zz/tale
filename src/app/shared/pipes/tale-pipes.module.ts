import { NgModule }      from '@angular/core';

import { MapToIterablePipe } from './map-to-iterable.pipe';

@NgModule({
    imports: [ 
    ],

    declarations: [ 
        MapToIterablePipe
    ],

    exports: [ 
        MapToIterablePipe
    ]
})
export class TalePipesModule { }